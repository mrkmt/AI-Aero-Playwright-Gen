# AI-Aero-Playwright-Gen: The Gold Standard Plan

This plan consolidates the "Hidden Gems" from 32 iterations into a premium local automation cockpit.

## 💎 Discovered Peak Components

1.  **Burmese DSL Engine** (`main.py` from `my_own`):
    - Re-implement the `qa_project_runner` with Burmese commands (`ဖွင့်`, `နှိပ်`, `ကြည့်ပေးပါ`).
    - Integrate with the core recorder to allow generating Burmese scripts.

2.  **Hybrid AI Pipeline** (`ai_26/27`):
    - Smart routing logic to manage token usage.
    - Local Ollama for simple tasks / Cloud Qwen for complex reasoning.

3.  **ISTQB & JIRA Templates** (`ai_25`):
    - Professional test case generation following international standards.
    - Standardized metadata (Preconditions, Priority, Risk Level).

4.  **Agentic Self-Healing** (`ai-k-os`):
    - Use AI Vision to automatically repair selectors during test playback.

## 🛠️ Phase 1: Local Foundation

- **Backend**: FastAPI + Python 3.10+ (Running via local venv).
- **Frontend**: React + Vite + TypeScript + Tailwind CSS (Running via npm).
- **Database**: PostgreSQL (Running locally).
- **Automation**: Playwright (Web) + Agentic Orchestrator.

## 🚀 Execution Strategy

1.  **Backend Core**:
    - [ ] Setup `main.py` entry point.
    - [ ] Integrate ported `burmese_engine.py` and `recorder_service.py`.
    - [ ] Initialize `agentic_service.py` for self-healing.

2.  **Frontend Dashboard**:
    - [ ] Create modern, dark-themed UI with glassmorphism effects.
    - [ ] Integrated `RecorderPage` for live sessions.
    - [ ] "Burmese Script" editor for visual automation.

3.  **Knowledge & RAG**:
    - [ ] Port the ISTQB knowledge base (PDF/JSON) into local ChromaDB.

---
*Created per request in [AI-Aero-Playwright-Gen/docs/PLAN_FINAL.md]*
